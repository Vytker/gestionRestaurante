_format_version: "3.0"
_transform: true

services:
  - name: reserva-api
    url: http://reserva_api:8080
    routes:
      - name: reserva-api-route
        paths:
          - /reservas
        strip_path: true
        methods:
          - GET
          - POST
          - PUT
          - DELETE
